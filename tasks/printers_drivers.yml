- name: Creates directory /srv/samba/printer_drivers
  file:
    path: /srv/samba/printer_drivers
    state: directory
    owner: root
    group: "{{ domain_member_realm }}\\Domain Admins" 
    mode: '2775'
    recurse: yes
